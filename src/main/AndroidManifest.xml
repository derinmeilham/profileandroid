<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.derin.tugasUTS">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.derin.tugasUTS.activity.OnBoardingActivity"
            android:theme="@style/theme2"></activity>
        <activity android:name="com.derin.tugasUTS.activity.MainActivity" />
        <activity
            android:name="com.derin.tugasUTS.activity.SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/theme2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>